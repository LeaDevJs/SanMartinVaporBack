spring.application.name=SanMartinVapor
# --- DATASOURCE (MySQL) ---
spring.datasource.url=jdbc:mysql://localhost:3306/sanmartinvapor?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=America/Argentina/Buenos_Aires&characterEncoding=UTF-8
spring.datasource.username=root
spring.datasource.password=Pity0912!
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Pool (opcional, seguro y simple)
spring.datasource.hikari.maximum-pool-size=10
spring.datasource.hikari.minimum-idle=2

# --- JPA / HIBERNATE ---
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.open-in-view=false

# --- SERVIDOR (opcional) ---
server.port=8080

